<UserControl x:Class="Gui.View.TargetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border BorderThickness="2" BorderBrush="CadetBlue" Margin="2">
            <StackPanel>
                <TextBlock Text="{Binding Target.Name}" FontSize="24"/>
                <TextBlock Text="{Binding Target.IsAlive}" FontSize="24"/>
                <Button Content="Kill!" Command="{Binding KillTargetCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>