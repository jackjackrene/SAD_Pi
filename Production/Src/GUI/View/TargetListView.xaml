<UserControl x:Class="GUI.View.TargetListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:GUI.View"
             xmlns:viewModel="clr-namespace:GUI.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        
        <!-- Set the Grid's Data context-->
        <Grid.DataContext>
            <viewModel:TargetListViewModel></viewModel:TargetListViewModel>
        </Grid.DataContext>
        
        <!-- Define two rows (no need for columns) -->
        <Grid.RowDefinitions>
            <RowDefinition Height="80*"></RowDefinition>
            <RowDefinition Height="20*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- Place the ListView in the first row -->
        <ListView Grid.Row="0" Margin="10,10,10,0">
            
        </ListView>
        
        <!-- Place the button in the second row -->
        <Button Grid.Row="1" Margin="10" Content="Clear All Targets">
        </Button>
        
    </Grid>
</UserControl>
